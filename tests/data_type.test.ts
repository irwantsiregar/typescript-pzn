describe("Data Type", () => {
  it("should must be declare", () => {
    const name: string = "Irwant";
    const balance: number = 1000000;
    const isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
