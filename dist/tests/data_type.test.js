"use strict";
describe("Data Type", () => {
    it("should must be declare", () => {
        const name = "Irwant";
        const balance = 1000000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
